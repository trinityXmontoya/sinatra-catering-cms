<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta content="IE=edge" http-equiv="X-UA-Compatible">
		<meta content="width=device-width, initial-scale=1" name="viewport">
		<meta content="" name="description">
		<meta content="" name="author">
		<title>Login</title>

    <%= css :admin %>
    <%= stylesheet_link_tag '//cdnjs.cloudflare.com/ajax/libs/font-awesome/4.2.0/css/font-awesome.min.css' %>
    <%= js :admin %>

		<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
		<!--[if lt IE 9]>
		<script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
		<script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
	<![endif]-->
	</head>
	<body>
		<div class="container">
			<div class="row">
				<div class="col-md-4 col-md-offset-4">
          <div id="login-error">
            <% if flash[:notice] %>
              <div class="alert alert-danger"><%= flash[:notice] %></div>
            <% end %>
          </div>
					<div class="login-panel panel panel-default">
						<div class="panel-heading">
							<h3 class="panel-title">
								Please Sign In
							</h3>
						</div>
						<div class="panel-body">
              <% form_tag '/login' do %>

                <%= label_tag :username %>
                <%= text_field_tag :username, name: 'admin[username]', class: 'form-control', autofocus: "" %>

                <%= label_tag :password %>
                <%= password_field_tag :password, name: 'admin[password]', class: 'form-control' %>
                <br>
                <%= submit_tag "Login", class:"btn btn-lg btn-success btn-block" %>
              <% end %>
						</div>
					</div>
				</div>
			</div>
		</div>
	</body>
</html>
